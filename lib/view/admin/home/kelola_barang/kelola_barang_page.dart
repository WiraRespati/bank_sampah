import 'package:flutter/material.dart';

class KelolaBarangPage extends StatelessWidget {
  const KelolaBarangPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Kelola Barang Page'),
      ),
    );
  }
}