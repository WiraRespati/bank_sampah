import 'package:flutter/material.dart';

class EditBarangPage extends StatelessWidget {
  const EditBarangPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Edit Barang Page'),
      ),
    );
  }
}