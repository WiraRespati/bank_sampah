import 'package:flutter/material.dart';

class UploadSampahPage extends StatelessWidget {
  const UploadSampahPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Upload Sampah Page'),
      ),
    );
  }
}
